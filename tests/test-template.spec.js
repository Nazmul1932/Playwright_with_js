const { chromium, test } = require('@playwright/test');
test('login-logout-using-codegen', async ({page}) => {

    await page.goto() // aikhan theke start korte hbe
    //aikhane baki code gula hbe
    await page.close() //aikhane sesh korte hbe

})